# buildmaster
npx create-next-app@latest buildmaster
cd buildmaster
git init
git add .
git commit -m "Initial Next.js app"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/buildmaster.git
git push -u origin main
